# Cloudflare Pages Redirects
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# NOTE:
# - Redirect `www -> apex` cannot use absolute source URLs in `_redirects`.
#   Configure it in Cloudflare dashboard using domain-level redirect / bulk redirect.
# - SPA fallback (`/* /index.html 200`) causes a loop warning in Pages and is not needed.
#   Cloudflare Pages handles SPA fallback automatically for this setup.
