# Cloudflare Pages Configuration
# https://developers.cloudflare.com/pages/configuration/wrangler-configuration/

name = "lapakbangade"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# Pages-specific configuration
[pages]
build_output_dir = "dist"

# Environment variables (set in Cloudflare Dashboard for secrets)
# EXCHANGE_RATE_API_KEY = "your-api-key" # Set in dashboard, not here

# Production environment
[env.production]
name = "lapakbangade"

# Preview/staging environment
[env.preview]
name = "lapakbangade-preview"
